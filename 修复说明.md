# vbox 三个问题修复说明

修复文件共 3 个，均为最小改动，**未触碰任何网盘资源解析链路**（CloudDriveManager / resolveBaiduPlayURLViaMainRoute / resolvePlayURL / 主路链→兜底 逻辑均未改动）。

---

## 问题 1：切集排序错乱（倒序播下一集仍播倒序）

**文件**：`vbox/Views/PlayerViewsV2.swift`

**改动**：
1. 新增持久状态字段（942 行附近）：
   ```swift
   @Published var episodesReversed = false
   ```
   把原本纯视图层的 `@State isReversed` 提升为 `PlayerState` 的持久属性，使"下一集"逻辑能感知排序方向。

2. 重写 `hasNextEpisode`（1450 行附近）：倒序时改为判断 `currentEpisodeIndex > 0`。

3. 重写 `playNextEpisode`（1470 行附近）：
   ```swift
   let step = episodesReversed ? -1 : 1
   let nextIndex = currentEpisodeIndex + step
   ```
   倒序下"下一集"改为数组前一个元素（`-1`），并补齐边界判断。

4. 横屏 `LandscapeEpisodePickerOverlay` 和竖屏 `EpisodePickerPopupWrapper` 里的局部 `@State isReversed` 全部改为绑定 `$playerState.episodesReversed`，删除了原来"切换排序后重新定位当前集"的冗余代码（那段代码本身有 bug，且逻辑已不需要）。

**效果**：切换倒序后点"下一集"会按倒序方向（集数更大）推进，切换正序后按正序推进，方向统一、不会错乱。

---

## 问题 2：MDK 切集黑屏（横屏有声无画面 / 竖屏卡上一帧）

**文件**：`vbox/PlayerCore/MDKRenderView.swift`

**根因**：切集时 `markReloading()` 清空纹理置黑，但 `MTKView.isPaused=true` 下不会自动 draw，而切集后 MDK 内部的 render callback 可能不再触发，`renderVideo()` 长期返回 `pts < 0`，形成"黑屏等新帧、新帧等 setNeedsDisplay"的死锁。

**改动**：
1. 新增 `reloadWakeTimer`（约每 16ms 主动 `setNeedsDisplay()`），在 `markReloading()` 时启动。
2. 定时器回调里判断超时（5 秒）则强制解除 `isReloading`，避免永久黑屏。
3. 当 `renderVideo() >= 0`（新帧到达）或 render callback 触发、或 `markReloadComplete()` 时，停止定时器并解除黑屏标记。

**效果**：切集后主动轮询直到首帧真正渲染，打破死锁，横屏/竖屏均能正常出画面。

---

## 问题 3：切换到 MPV 内核闪退

**文件**：`vbox/PlayerCore/LibmpvMoltenVKPlayerCore.swift`

**根因**：`teardown()` 在主线程直接 `mpv_terminate_destroy(handle)`，而 `readEvents()` 在 `eventQueue` 后台线程可能正阻塞/访问同一 `handle`，导致 use-after-free 崩溃；且原 `guard !isShuttingDown || mpv != nil` 逻辑有缺陷，可能重复销毁。

**改动**：
1. 将 `mpv_terminate_destroy` 移入 `eventQueue.sync` 串行队列执行，与 `readEvents` 的事件处理串行化，保证销毁前后台线程已停止访问该 handle。
2. 销毁前在串行队列内发送 `stop` 命令（幂等），并加 `self.mpv == handle` 快照校验，防止重复销毁。
3. `eventQueue.sync` 天然等待当前事件批次处理完毕再销毁，替代了原来不可靠的 `usleep(50000)` + `DispatchGroup.wait(0.1)`。

**效果**：消除 MPV 切换/切集时的 use-after-free 竞态，显著降低闪退概率。

---

## ⚠️ 需要真机验证的点

1. **MDK 黑屏**：改动方向正确，但黑屏是否彻底消失需真机验证（mdk-sdk 内部行为无法在本环境确认）。
2. **MPV 闪退**：native 崩溃无法在 Linux 环境复现。本次修复了代码中可见的竞态，但若仍闪退，请提供 `.ips` 崩溃日志（设置 → 隐私 → 分析与改进 → 分析数据），以便定位残留崩溃点。

## 测试建议

1. **排序**：进入任一多集资源，切换倒序播放第 2 集，点"下一集"应播第 1 集（倒序方向）；切回正序后点"下一集"应播第 3 集（正序方向）。
2. **MDK**：用 MDK 内核（MKV/百度原画等），横屏和竖屏分别切集，确认画面正常、无黑屏、无卡上一帧。
3. **MPV**：播放任一资源，手动切换到 MPV 内核，确认不闪退；再从 MPV 切回其它内核再切回 MPV，反复测试稳定性。

**网盘资源回归测试**：夸克/百度网盘播放、切集、切内核，确认播放路链不受影响、视频能正常播放。
